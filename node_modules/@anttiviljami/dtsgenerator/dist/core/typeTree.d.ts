import { Schema } from './type';
export interface TypeTree {
    children: Map<string, TypeTree>;
    schema?: Schema;
}
export declare function buildTypeTree(schemas: IterableIterator<Schema>): TypeTree;
